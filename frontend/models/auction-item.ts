export interface AuctionItem {
  id: string;
  name: string;
  model: string;
  serialNumber: string;
  price: number;
}


export interface CSVItem {
  nummer?: string;
  model?: string;
  seriennummer?: string;
  price?: number;
}